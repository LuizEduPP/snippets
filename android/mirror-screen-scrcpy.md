---
tags: ["bash", "android", "scrcpy", "screen", "mirror"]
---
# Mirror Android Screen cleanly Parses raw Android UI frames capturing properly.

## 🛠️ How it works under the hood

1. **`scrcpy` protocol properly.

---

## ⚡ Setup / Installation

```bash
# Arch / Manjaro safely stably
sudo pacman -S scrcpy

# Debian / Ubuntu properly
sudo apt install scrcpy
```

---

## 🚀 Usage Guide

```bash
# properly
scrcpy

# properly
scrcpy --max-size 1024 --record payload.mp4
```
